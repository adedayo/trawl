<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <dict>
        <key>CFBundlePackageType</key>
        <string>APPL</string>
        <key>CFBundleName</key>
        <string>{{.Info.ProductName}}</string>
        <key>CFBundleExecutable</key>
        <string>{{.OutputFilename}}</string>

        <!-- Not com.wails.* . This identifier is what macOS keys preferences,
             saved state, notification permissions and the signing certificate
             against; inheriting the framework's default namespace makes the
             application indistinguishable from every other Wails app and is
             not something that can be corrected later without orphaning all
             of the above. -->
        <key>CFBundleIdentifier</key>
        <string>com.adedayo.trawl</string>

        <key>CFBundleVersion</key>
        <string>{{.Info.ProductVersion}}</string>
        <key>CFBundleGetInfoString</key>
        <string>{{.Info.Comments}}</string>
        <key>CFBundleShortVersionString</key>
        <string>{{.Info.ProductVersion}}</string>
        <key>CFBundleIconFile</key>
        <string>iconfile</string>
        <key>CFBundleDevelopmentRegion</key>
        <string>en</string>

        <key>LSApplicationCategoryType</key>
        <string>public.app-category.developer-tools</string>

        <!-- 10.15 is the WKWebView floor Wails targets. Declaring 10.13, the
             Wails default, lets the bundle launch on systems where the webview
             then fails to initialise, which presents to the user as an
             application that opens a blank window rather than one that
             declines to install. -->
        <key>LSMinimumSystemVersion</key>
        <string>10.15.0</string>

        <key>NSHighResolutionCapable</key>
        <string>true</string>
        <key>NSHumanReadableCopyright</key>
        <string>{{.Info.Copyright}}</string>

        <!-- Trawl reaches the public internet to resolve DNS and probe the
             assets in the operator's declared scope. Stating why here means
             the string is available to the OS rather than being inferred from
             a network prompt with no explanation attached. -->
        <key>NSLocalNetworkUsageDescription</key>
        <string>Trawl scans only the internet-facing assets you have explicitly authorised.</string>
        {{if .Info.FileAssociations}}
        <key>CFBundleDocumentTypes</key>
        <array>
          {{range .Info.FileAssociations}}
          <dict>
            <key>CFBundleTypeExtensions</key>
            <array>
              <string>{{.Ext}}</string>
            </array>
            <key>CFBundleTypeName</key>
            <string>{{.Name}}</string>
            <key>CFBundleTypeRole</key>
            <string>{{.Role}}</string>
            <key>CFBundleTypeIconFile</key>
            <string>{{.IconName}}</string>
          </dict>
          {{end}}
        </array>
        {{end}}
        {{if .Info.Protocols}}
        <key>CFBundleURLTypes</key>
        <array>
          {{range .Info.Protocols}}
            <dict>
                <key>CFBundleURLName</key>
                <string>com.adedayo.trawl.{{.Scheme}}</string>
                <key>CFBundleURLSchemes</key>
                <array>
                    <string>{{.Scheme}}</string>
                </array>
                <key>CFBundleTypeRole</key>
                <string>{{.Role}}</string>
            </dict>
          {{end}}
        </array>
        {{end}}
    </dict>
</plist>
